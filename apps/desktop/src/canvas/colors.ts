// Canvas color/font tokens — re-export the generated theme mirror so Pixi
// stays in lockstep with CSS `--lt-*` (see scripts/compile-theme.mjs).
export {
  PANEL,
  BG_RAISE,
  BORDER,
  LINE,
  LINE_STRONG,
  AMBER,
  AMBER_BRIGHT,
  AMBER_DEEP,
  AMBER_WASH,
  TEXT,
  TEXT_SOFT,
  MUTED,
  FAINT,
  FONT_UI,
  FONT_MONO,
  FONT_DISPLAY,
} from "../theme-tokens";
