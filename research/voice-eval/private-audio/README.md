# Private audio fixtures

Drop real-microphone WAVs here for local evaluation. Everything under this
directory is gitignored except this README and `.gitkeep`.

Expected format for FluidAudio paths: **16 kHz mono Float32 WAV** (`LEF32@16000`).

Register new files in `../manifest.yaml` with `optional: true` until the audio
is present on the measurement Mac.
