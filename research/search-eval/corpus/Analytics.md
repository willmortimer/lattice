# Analytical Datasets

`Events.dataset` packages Hive-partitioned Parquet under `facts/` plus an
`annotations.sqlite` overlay. Desktop Preview uses Perspective; Chart uses
Vega-Lite; Profile runs DuckDB `SUMMARIZE`.
