# Data Applications

`.data` packages store SQLite tables with YAML schema, views, forms, and
actions. CRM layouts include board, gallery, calendar, and form. Lookup and
rollup fields resolve at read time over relation columns.
