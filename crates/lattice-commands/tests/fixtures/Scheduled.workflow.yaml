format: lattice-workflow
version: 1
name: Hourly scheduled reminder
enabled: true
trigger:
  type: schedule
  interval_seconds: 3600
steps:
  - id: note
    action: notification
    with:
      message: Scheduled workflow tick (fired by latticed interval runner when due)
