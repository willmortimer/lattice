[package]
name = "lattice-mcp-catalog"
description = "Canonical workspace.* MCP tool catalog shared by daemon and cloud"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
